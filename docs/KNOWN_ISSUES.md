# Known issues (v0.1.0)

- تماس بین دو مرورگر جدا Relayed P2P کامل نیست؛ حلقهٔ محلی است.
- Store فایل JSON برای مقیاس افقی محدود است.
- Formatter جدا از ESLint (Prettier) در repo نیست.
- Vitest فایل‌های موازی اگر `VITEST_WORKER_ID` خالی باشد Store را شریک می‌شوند؛ runner فعلی worker id می‌گذارد.

گزارش باگ: `.github/ISSUE_TEMPLATE/bug.yml`.
