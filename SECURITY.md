# Security reports

آسیب‌پذیری را در Issue عمومی با PoC مخرب نگذار.

از مسیر داخل محصول Settings → Security (گزارش آسیب‌پذیری) یا کانال خصوصی تیم ایمنی نیکسو استفاده کن.

شامل: نسخهٔ اپ (`/api/version`)، مسیر API، اثر، بدون دادهٔ واقعی کاربر و بدون Secret.

Rotate کلید فقط از env روی سرور؛ هرگز کلید را در تیکت نچسبان.
